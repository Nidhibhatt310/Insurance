resources:
  jobs:
    my_kafka_job:
      name: Kafka Consumer Job
      tasks:
        - task_key: python_wheel_upload
          python_wheel_task: 
            package_name: insurance
            entry_point: main
          job_cluster_key: task1_cluster
          libraries:
            - whl: ../dist/*.whl
        - task_key: consume_task
          depends_on:
            - task_key: python_wheel_upload
          notebook_task:
            notebook_path: "../src/insurance/ingestion/kafka_consumer.py"
            source: WORKSPACE
          job_cluster_key: task1_cluster
      job_clusters:
        - job_cluster_key: task1_cluster
          new_cluster:
            spark_version: 17.3.x-scala2.13
            node_type_id: Standard_DC4as_v5
            spark_conf:
              # Syntax: {{secrets/scope_name/secret_name}}
              bootstrap_server: "{{secrets/insurance-kafka-scope-dev/kafka-bootstrap-server-dev}}"
              kafka_key: "{{secrets/insurance-kafka-scope-dev/kafka-key-dev}}"
              kafka_secret: "{{secrets/insurance-kafka-scope-dev/kafka-secret-dev}}"
            spark_env_vars:
              PYSPARK_PYTHON: /databricks/python3/bin/python3
            data_security_mode: DATA_SECURITY_MODE_STANDARD
            runtime_engine: STANDARD
            kind: CLASSIC_PREVIEW
            is_single_node: true
      queue:
        enabled: true
    